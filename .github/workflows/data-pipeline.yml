name: Quarterly Data Update

on:
  schedule:
    - cron: "0 6 15 1,4,7,10 *"
  workflow_dispatch:
    inputs:
      rebuild_all:
        description: "Rebuild all data from scratch"
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install pandas openpyxl numpy requests beautifulsoup4 pdfplumber

      - name: Download raw data
        run: python scripts/download_data.py

      - name: Preprocess data
        run: python scripts/preprocess_data.py

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet && echo "changed=false" >> "$GITHUB_OUTPUT" || echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          git commit -m "chore: update quarterly crime data $(date +%Y-%m-%d)"
          git push
